# Crochetadas

Site de receitas de crochê. Projeto em React + Vite.

## Rodar localmente (opcional, precisa de Node.js instalado)
npm install
npm run dev

## Build para produção
npm install
npm run build
(gera a pasta dist/, pronta para qualquer hospedagem estática)
